# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
uag
uag.exe

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# IDE - JetBrains
.idea/
*.iml
*.iws
*.ipr

# IDE - Cursor
.cursor/

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Logs
*.log
logs/
*.log.*

# Temporary files
tmp/
temp/
*.tmp
*.swp
*.swo
*~

# Build artifacts
build/
dist/
bin/
out/

# Configuration overrides (keep template, ignore local overrides)
config/local.yaml
config/*.local.yaml
.env
.env.local

# Kubernetes secrets (never commit secrets!)
deploy/*-secret.yaml
deploy/secrets/

# Test coverage
coverage.txt
coverage.html
*.coverprofile

# Profiling data
*.prof
*.pprof
cpu.prof
mem.prof

# Air live reload (if using)
.air.toml
tmp/

# Delve debugger
__debug_bin

# eBPF compiled objects and generated Go bindings
*.o
*.ll
pkg/ebpf/bpf_bpfel.go
pkg/ebpf/bpf_bpfeb.go
pkg/ebpf/bpf_bpfel.o
pkg/ebpf/bpf_bpfeb.o

# Keep vendored BPF headers (DO NOT ignore)
!pkg/ebpf/include/

